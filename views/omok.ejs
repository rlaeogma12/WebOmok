<!doctype html>
<html>
  <head>
    <title>오목 플레이</title>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { font: 13px Helvetica, Arial; }
      form { background: #000; padding: 3px; position: fixed; bottom: 60%; width: 30%; }
      form input { border: 0; padding: 10px; width: 90%; margin-right: .5%; }
      form button { width: 9%; background: rgb(130, 224, 255); border: none; padding: 10px; }
      #messages { list-style-type: none; margin: 0; padding: 0; position: fixed; width: 100%}
      #messages li { width: 30%; padding: 5px 10px; }
      #messages li:nth-child(odd) { background: #eee; }
      img { width: 50%; position:fixed; left:35%;}
      h1 {font: 10px; position:fixed; top : 45%; left:3%;}
      #omokinfo { list-style-type: none; margin: 0; padding: 0; position: fixed; right: 5%;}

    </style>

  </head>
  <body>
    <ul id="messages"></ul>
    <img src="../images/omok.jpg"/>

    <h1 class="userinfo">
      User ID : <input id="user_id">
    </h1>

    <form action="">
      <input id="m" autocomplete="off" /><button>Send</button>
      <script src="http://code.jquery.com/jquery-1.11.0.min.js"></script>
      <script src="/socket.io/socket.io.js"></script>
      <script>
      var userid = prompt("사용할 아이디를 입력하세요", "guest1");
      $('#user_id').val(userid);

      var socket = io();
      $('form').submit(function(){
        socket.emit('chat message', userid + ' : ' + $('#m').val());
        $('#m').val('');

        return false;
      });
      socket.on('chat message', function(msg){
        $('#messages').append($('<li>').text(msg));
      });
      </script>
    </form>


    <ul id="omokinfo">
      <li>
        guest1(흑) vs guest2(백)
      </li>
      <li>
        현재 턴
      </li>
      <li>
        <input id="turn" value = "흰색">
      </li>
    </ul>

  </body>
</html>
